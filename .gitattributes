# 统一换行符。
#
# 这个项目里有需要在 Linux 上执行的 shell 脚本（deploy/*.sh），
# 如果它们被检出成 CRLF，Linux 会报 "bad interpreter: /bin/bash^M"。
# 所以这里强制所有文本文件在仓库里和检出时都用 LF，
# 不管克隆的人用的是 Windows 还是 Linux。
* text=auto eol=lf

# 下面这些是二进制，别做任何换行转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
*.woff binary
*.woff2 binary
*.ttf binary
*.otf binary
*.zip binary
*.gz binary

# 明确标出语言，让 GitHub 的语法高亮和统计更准
*.sh text eol=lf
Dockerfile text eol=lf
*.caddyfile text eol=lf
Caddyfile text eol=lf

# Windows 批处理必须是 CRLF。上面那条 `* eol=lf` 会把它一起检出成 LF，
# 而 cmd.exe 解析 LF 结尾的 .cmd 会错位：注释被当成命令执行、续行符 ^
# 失效，脚本根本跑不起来 —— deploy/update-vps.cmd 就踩了这个坑。
*.cmd text eol=crlf
*.bat text eol=crlf
